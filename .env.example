# 火山引擎方舟 API Key（勿将真实密钥写入仓库；复制本文件为 .env 后在本地填写）
ARK_API_KEY=

# OpenAI 兼容接入点的 Base URL。若留空或未设置，将使用方舟北京区域默认值（见 src/config.ts 中的 DEFAULT_ARK_BASE_URL）
ARK_BASE_URL=

# 对话（Chat）模型在方舟控制台对应的 Endpoint / 模型 ID
ARK_CHAT_MODEL=

# 文本向量（Embeddings）模型在方舟控制台对应的 Endpoint / 模型 ID
ARK_EMBED_MODEL=
